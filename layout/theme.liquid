{% assign seoTitle = '' %}
{% if product.seoTitle %}
    {% assign seoTitle = product.seoTitle %}
{% elsif search.results[0].categories[0].title %}
    {% assign seoTitle = search.results[0].categories[0].title %}
{% elsif seo.title %}
    {% assign seoTitle = seo.title %}
{% else %}
    {% assign seoTitle = 'Site Do Ar | Ar-Condicionado com os Melhores Preços em SP' %}
{% endif %}

{% assign seoDescription = '' %}
{% if product.seoDescription %}
    {% assign seoDescription = product.seoDescription %}
{% elsif search.results[0].categories[0].description %}
    {% assign seoDescription = search.results[0].categories[0].description %}
{% elsif seo.description %}
    {% assign seoDescription = seo.description %}
{% elsif store.description %}
    {% assign seoDescription = store.description %}
{% endif %}

{% assign seoKeywords = '' %}
{% if product.tags %}
    {% assign seoKeywords = product.tags %}
{% elsif search.results[0].categories[0].tags %}
    {% assign seoKeywords = search.results[0].categories[0].tags %}
{% elsif seo.keywords %}
    {% assign seoKeywords = seo.keywords %}
{% elsif store.keywords %}
    {% assign seoKeywords = store.keywords %}
{% endif %}

{% assign seoImage = '' %}
{% assign seoImageType = '' %}
{% assign seoImageWidth = '' %}
{% assign seoImageHeight = '' %}
{% if product.medias[0].path %}
    {% assign seoImage = product.medias[0].path | imgUrl: 200 %}
    {% if product.medias[0].mimeType %}
        {% assign seoImageType = product.medias[0].mimeType %}
    {% endif %}
    {% if product.medias[0].width %}
        {% assign seoImageWidth = product.medias[0].width %}
    {% endif %}
    {% if product.medias[0].height %}
        {% assign seoImageHeight = product.medias[0].height %}
    {% endif %}
{% elsif theme.logo.path %}
    {% assign seoImage = theme.logo.path | imgUrl %}
    {% if theme.logo.file.mimeType %}
        {% assign seoImageType = theme.logo.file.mimeType %}
    {% endif %}
    {% if theme.logo.width %}
        {% assign seoImageWidth = theme.logo.width %}
    {% endif %}
    {% if theme.logo.height %}
        {% assign seoImageHeight = theme.logo.height %}
    {% endif %}
{%endif%}

<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,minimum-scale=1">
    <title>{{ seoTitle }}</title>
    <meta http-equiv="Content-Language" content="pt-br">
    <meta name="description" content="{{ seoDescription }}">
    <meta name="keywords" content="{{ seoKeywords }}">
    <meta name="theme-color" content="{{ themeConfig.color }}"/>
    <link rel="icon" href="{{ theme.favicon.path | imgUrl: 48 }}" sizes="48x48" type="image/webp">
    <link rel="shortcut icon" href="{{ theme.favicon.path | imgUrl: 48 }}" type="image/webp">
    <link rel="apple-touch-icon" href="{{ theme.favicon.path | imgUrl: 180 }}" sizes="180x180">
    <link rel="preconnect" href="https://salescdn.net" crossorigin>
    <link rel="preload" href="{{ 'assets/theme.js' | themeAssetUrl }}" as="script">
    <script src="{{ 'assets/theme.js' | themeAssetUrl }}"></script>
    <link media="all" type="text/css" rel="stylesheet" href="{{ 'assets/theme-plugins.css' | themeAssetUrl }}">
    <link media="all" type="text/css" rel="stylesheet" href="{{ 'assets/theme-all.css' | themeAssetUrl }}">
    <link media="all" type="text/css" rel="stylesheet" href="{{ 'assets/theme-custom.css' | themeAssetUrl }}">
    {%- comment -%}
      Custom CSS (ordem importa):
      1. mega-menu.css — cards, nav, hover, mega menu
      2. header-v2.css — layout header 3 faixas
      3. favorites.css — coração, página favoritos, contador
      4. product-compare.css — comparador
      5. product-hover-image.css — hover 2ª foto nos cards
    {%- endcomment -%}
    <link media="all" type="text/css" rel="stylesheet" href="{{ 'assets/mega-menu.css' | themeAssetUrl }}">
    <link media="all" type="text/css" rel="stylesheet" href="{{ 'assets/header-v2.css' | themeAssetUrl }}">
    <link media="all" type="text/css" rel="stylesheet" href="{{ 'assets/favorites.css' | themeAssetUrl }}">
    <link media="all" type="text/css" rel="stylesheet" href="{{ 'assets/product-compare.css' | themeAssetUrl }}">
    <link media="all" type="text/css" rel="stylesheet" href="{{ 'assets/product-hover-image.css' | themeAssetUrl }}">
    {%- comment -%}
      HEADER-ORANGE-INLINE-v5 — override definitivo (vence theme-all + theme-custom).
      Publicar layout/theme.liquid no WDNA; não depende de colar CSS em arquivos separados.
    {%- endcomment -%}
    <style id="header-orange-inline-v5">
/* Badge carrinho laranja (vence theme-custom #61bb47) */
body .header:not(.header-checkout) .header-link_cart .cart_quantity,
body .header:not(.header-checkout) .cart_link .cart_quantity,
body .header:not(.header-checkout) .cart_quantity,
body .header:not(.header-checkout) [data-cart-quantity].cart_quantity,
body .header:not(.header-checkout) .header-link_cart [data-cart-quantity],
body .header:not(.header-checkout) .header-v2-action--cart .cart_quantity,
body .header:not(.header-checkout) .header-v2-action--cart [data-cart-quantity] {
  background-color: #f97316 !important;
  background: #f97316 !important;
  border-color: #ea580c !important;
  color: #ffffff !important;
}
body .header:not(.header-checkout) .header-link_cart svg,
body .header:not(.header-checkout) .header-link_cart .icon_cart,
body .header:not(.header-checkout) .cart_link svg,
body .header:not(.header-checkout) .header-v2-action--cart svg {
  fill: #4b96d4 !important;
}
body .header:not(.header-checkout) .header a.header-link_cart:hover,
body .header:not(.header-checkout) .header a.header-link_cart:focus,
body .header:not(.header-checkout) .header a.cart_link:hover,
body .header:not(.header-checkout) .header a.cart_link:focus,
body .header:not(.header-checkout) .header-link_cart:hover,
body .header:not(.header-checkout) .header-link_cart:focus,
body .header:not(.header-checkout) .cart_link:hover,
body .header:not(.header-checkout) .cart_link:focus,
body .header:not(.header-checkout) .header-v2-action--cart:hover,
body .header:not(.header-checkout) .header-v2-action--cart:focus {
  color: #f97316 !important;
  opacity: 1 !important;
}
body .header:not(.header-checkout) .header-link_cart:hover svg,
body .header:not(.header-checkout) .header-link_cart:focus svg,
body .header:not(.header-checkout) .header-link_cart:hover .icon_cart,
body .header:not(.header-checkout) .header-link_cart:focus .icon_cart,
body .header:not(.header-checkout) .cart_link:hover svg,
body .header:not(.header-checkout) .cart_link:focus svg,
body .header:not(.header-checkout) .header-v2-action--cart:hover svg,
body .header:not(.header-checkout) .header-v2-action--cart:focus svg {
  fill: #f97316 !important;
}
/* Header v2 — ícones outline e caixas de ação (vence theme-all) */
body .header.header-v2 .header-v2-action__icon svg.sda-ico-stroke,
body .header.header-v2 .header-v2-action__icon .sda-ico-stroke path,
body .header.header-v2 .header-v2-action__icon .sda-ico-stroke circle {
  fill: none !important;
  stroke: #4b96d4 !important;
}
body .header.header-v2 a.header-v2-action,
body .header.header-v2 a.header-v2-action:hover,
body .header.header-v2 a.header-v2-action:focus {
  color: #4b96d4 !important;
}
body .header.header-v2 .header-v2-action__label {
  color: #4b96d4 !important;
  font-size: 11px !important;
  font-weight: 700 !important;
}
body .header.header-v2 > .nav-content.content {
  background: linear-gradient(180deg, #f6f9fc 0%, #eef3f8 100%) !important;
}
body .header.header-v2 .nav-content .nav-main_text {
  color: #1a2d4a !important;
}
/* Faixa utilitária — Akhand + tamanho mock (vence theme-all) */
body .header.header-v2 .header-utility,
body .header.header-v2 .header-utility a,
body .header.header-v2 .header-utility button,
body .header.header-v2 .header-utility .header-utility__label {
  font-family: Akhand, sans-serif !important;
  font-size: 12px !important;
  font-weight: 600 !important;
  letter-spacing: 0.14em !important;
  text-transform: uppercase !important;
  color: #fff !important;
}
body .header.header-v2 .header-utility {
  background: #0c1f3d !important;
}
body .header.header-v2 .header-utility .header-utility__inner {
  min-height: 0 !important;
  padding-top: 4px !important;
  padding-bottom: 4px !important;
}
body .header.header-v2 .header-v2-nav-row.nav-content,
body .header.header-v2 > .nav-content.content.header-v2-nav-row {
  padding-top: 0 !important;
  padding-bottom: 0 !important;
  min-height: 0 !important;
}
body .header.header-v2 .header-v2-nav-inner {
  min-height: 0 !important;
  padding-top: 2px !important;
  padding-bottom: 2px !important;
}
body .header:not(.header-checkout) .header-link_cart:hover .cart_quantity,
body .header:not(.header-checkout) .header-link_cart:focus .cart_quantity,
body .header:not(.header-checkout) .cart_link:hover .cart_quantity,
body .header:not(.header-checkout) .cart_link:focus .cart_quantity,
body .header:not(.header-checkout) .header-link_cart:hover [data-cart-quantity],
body .header:not(.header-checkout) .header-link_cart:focus [data-cart-quantity],
body .header:not(.header-checkout) .header-v2-action--cart:hover [data-cart-quantity],
body .header:not(.header-checkout) .header-v2-action--cart:focus [data-cart-quantity] {
  background-color: #ea580c !important;
  background: #ea580c !important;
  border-color: #c2410c !important;
  color: #ffffff !important;
}
/* Conta + telefone — normal escuro, hover laranja (vence .header a:hover verde) */
body .header:not(.header-checkout) .header-box_account .header-link_text .account_link,
body .header:not(.header-checkout) .header-box_account .header-link_text a.account_link,
body .header:not(.header-checkout) .header-link_account .header-link_text .account_link,
body .header:not(.header-checkout) .header-link_account .header-link_text a.account_link {
  color: #101010 !important;
}
body .header:not(.header-checkout) .header-link-account_info .header-link_icon svg,
body .header:not(.header-checkout) .header-link-account_tel .header-link_icon svg {
  fill: #4b96d4 !important;
}
body .header:not(.header-checkout) .header-box_account .header-link_text a.account_link:hover,
body .header:not(.header-checkout) .header-box_account .header-link_text a.account_link:focus,
body .header:not(.header-checkout) .header-link_account .header-link_text a.account_link:hover,
body .header:not(.header-checkout) .header-link_account .header-link_text a.account_link:focus,
body .header:not(.header-checkout) .header a.account_link:hover,
body .header:not(.header-checkout) .header a.account_link:focus {
  color: #101010 !important;
  opacity: 1 !important;
}
body .header:not(.header-checkout) .header-box-account-info .header-link_text a.account_link:last-child:hover,
body .header:not(.header-checkout) .header-box-account-info .header-link_text a.account_link:last-child:focus,
body .header:not(.header-checkout) .header-link-account_info .header-link_text a.account_link:last-child:hover,
body .header:not(.header-checkout) .header-link-account_info .header-link_text a.account_link:last-child:focus,
body .header:not(.header-checkout) .header-link-account_info:hover .header-link_text a.account_link:last-child,
body .header:not(.header-checkout) .header-box-account-info:hover .header-link_text a.account_link:last-child {
  color: #f97316 !important;
}
body .header:not(.header-checkout) .header-box-account_tel .header-link_text a.account_link:first-child:hover,
body .header:not(.header-checkout) .header-box-account_tel .header-link_text a.account_link:first-child:focus,
body .header:not(.header-checkout) .header-link-account_tel .header-link_text a.account_link:first-child:hover,
body .header:not(.header-checkout) .header-link-account_tel .header-link_text a.account_link:first-child:focus,
body .header:not(.header-checkout) .header-link-account_tel:hover .header-link_text a.account_link:first-child,
body .header:not(.header-checkout) .header-box-account_tel:hover .header-link_text a.account_link:first-child {
  color: #f97316 !important;
}
body .header:not(.header-checkout) .header-link-account_info:hover .header-link_icon svg,
body .header:not(.header-checkout) .header-link-account_info:focus-within .header-link_icon svg,
body .header:not(.header-checkout) .header-link-account_tel:hover .header-link_icon svg,
body .header:not(.header-checkout) .header-link-account_tel:focus-within .header-link_icon svg {
  fill: #f97316 !important;
}
    </style>
    <link rel="preload" as="font" href="https://salescdn.net/static/fonts/akhand2/akhand-regular.woff2" type="font/woff2" crossorigin="anonymous">
    <link rel="preload" as="font" href="https://salescdn.net/static/fonts/akhand2/akhand-bold.woff2" type="font/woff2" crossorigin="anonymous">
    <link rel="preload" as="font" href="https://salescdn.net/static/fonts/akhand2/akhand-semibold.woff2" type="font/woff2" crossorigin="anonymous">
    <link rel="preload" as="font" href="https://salescdn.net/static/fonts/akhand2/akhand-black.woff2" type="font/woff2" crossorigin="anonymous">
    <meta name="twitter:card" content="summary"/>
    <meta name="twitter:title" content="{{ seoTitle }}"/>
    <meta name="twitter:image" content="{{ seoImage }}"/>
    <meta name="twitter:url" content="{{ store.url }}"/>
    <meta name="twitter:description" content="{{ seoDescription }}">
    <meta property="og:locale" content="pt-BR"/>
    <meta property="og:title" content="{{ seoTitle }}"/>
    <meta property="og:type" content="website"/>
    <meta property="og:url" content="{{ store.url }}"/>
    <meta property="og:image" content="{{ seoImage }}"/>
    <meta property="og:image:width" content="{{ seoImageWidth }}"/>
    <meta property="og:image:height" content="{{ seoImageHeight }}"/>
    {% if seoImageType != '' %}<meta property="og:image:type" content="{{ seoImageType }}"/>{% endif %}
    <meta property="og:site_name" content="{{ seoTitle }}"/>
    <meta property="og:description" content="{{ seoDescription }}"/>
    <meta name="csrf-token" content="{{ csrfToken }}">
    {%- for script in scripts.header -%}
        {{ script.code }}
    {%- endfor -%}
    {% block head %}{% endblock %}
</head>
<script>window.__SITE_DOAR_CUSTOMER__ = {{ customer | json }};</script>
<body x-data="setupStore()" x-init="init()" x-on:change-customer-login.window="changeCustomerLogin($event.detail.customer)" x-on:check-login-logged.window="checkLoginLogged()">
{%- for script in scripts.content -%}
    {{ script.code }}
{%- endfor -%}
<span class="sidenav-overlay_background" onclick="changeSidenavOverlayBackground()"></span>
<span class="loading-main loading"></span>
{% render 'sections/check-cookie'%}
{% render 'sections/sidenav-overlay-cart', cart: cart, store: store, themeConfig: themeConfig %}
{% render 'sections/sidenav-overlay-favorites' %}
{% render 'sections/mega-menu-ar' %}
{% render 'sections/product-compare-panel' %}
{% block content %}{% endblock %}
{% render 'sections/theme-schema', store: store, theme: theme, seoKeywords: seoKeywords, seoDescription: seoDescription, social: social %}
<script>
    window.onunload = function(){};
    window.onload = function() {
        loadFilesJSDefer();
        loadInputMask();
    }
    function loadFilesJSDefer() {
        var all_defer_script = document.createElement('script');
        all_defer_script.setAttribute('src', '{{ 'assets/theme-defer.js' | themeAssetUrl }}');
        document.querySelector('body').appendChild(all_defer_script);
    }
    captureUrlParameter();
    function captureUrlParameter() {
        const urlParams = new URLSearchParams(window.location.search);
        let sellerInit = null;
        let linkSellerInit = null;
        let refInit = null;
        urlParams.forEach((value, key) => {
            if (key.toLowerCase() === 'seller') sellerInit = value;
            if (key.toLowerCase() === 'linkseller') linkSellerInit = value;
            if (key.toLowerCase() === 'ref') refInit = value;
        });
        if (linkSellerInit) {
            localStorage.setItem('linkSeller', linkSellerInit);
            localStorage.removeItem('seller');
            localStorage.removeItem('ref');
        } else if (sellerInit) {
            localStorage.setItem('seller', sellerInit);
            localStorage.removeItem('linkSeller');
            localStorage.removeItem('ref');
        } else if (refInit) {
            localStorage.setItem('ref', refInit);
            localStorage.removeItem('seller');
            localStorage.removeItem('linkSeller');
        }
    }
    window.currencyStore = {% if store.symbolPrice %}"{{ store.symbolPrice }}"{% else %}null{% endif %};
</script>
<script src="{{ scripts.main.src }}"></script>
{%- comment -%} mega-menu.js define __SDA_HOVER_V2__; product-hover-image.js desliga se já ativo {%- endcomment -%}
<script src="{{ 'assets/header-v2-mobile.js' | themeAssetUrl }}" defer></script>
<script src="{{ 'assets/mega-menu.js' | themeAssetUrl }}" defer></script>
<script src="{{ 'assets/favorites.js' | themeAssetUrl }}" defer></script>
<script src="{{ 'assets/product-compare.js' | themeAssetUrl }}" defer></script>
<script src="{{ 'assets/product-hover-image.js' | themeAssetUrl }}" defer></script>
<script>
  window.addEventListener('load', function () {
    if (typeof window.initMegaMenuAr === 'function' && !document.querySelector('.mega-menu-ar')) {
      window.initMegaMenuAr();
    }
    if (window.SiteDoarFavorites && typeof window.SiteDoarFavorites.mergeGuestOnLogin === 'function') {
      window.SiteDoarFavorites.mergeGuestOnLogin();
      window.SiteDoarFavorites.refresh();
    }
  });
</script>
<script>
    function setupStore() {
        return {
            data: { isLogged: null, user: '' },
            init: function initStore() {
                var c = window.__SITE_DOAR_CUSTOMER__;
                if (c) {
                    this.changeCustomerLogin(c);
                } else {
                    this.data.isLogged = false;
                    this.data.user = '';
                }
            },
            headerLogout: function headerLogout() {
                StoreSDK.request.post('/ajax/logout').then((res) => {
                    window.location.href = window.location.href
                });
            },
            changeCustomerLogin: function changeCustomerLogin(customer) {
                this.data.isLogged = !!customer;
                if (customer) {
                    this.data.user = customer.name || customer.fantasyName || customer.email || '';
                } else {
                    this.data.user = '';
                }
                window.__SITE_DOAR_CUSTOMER__ = customer || null;
                if (typeof window.sdaUpdateUtilityBarLabels === 'function') {
                    window.sdaUpdateUtilityBarLabels(customer);
                }
                if (typeof window.sdaSyncMobileCatalogDock === 'function') {
                    window.sdaSyncMobileCatalogDock();
                }
                if (window.SiteDoarFavorites && typeof window.SiteDoarFavorites.mergeGuestOnLogin === 'function') {
                    window.SiteDoarFavorites.mergeGuestOnLogin();
                }
                if (this.data.isLogged) {
                    var elsShowcaseBtnLoginBuy = document.querySelectorAll('.login-btn_logged');
                    for (var i = 0; i < elsShowcaseBtnLoginBuy.length; i++) {
                        elsShowcaseBtnLoginBuy[i].classList.remove('d-hide');
                    }
                } else {
                    var elsShowcasePriceLogin = document.querySelectorAll('.login-btn_not_logged');
                    for (var i = 0; i < elsShowcasePriceLogin.length; i++) {
                        elsShowcasePriceLogin[i].classList.remove('d-hide');
                    }
                }
            },
            openSideNavOverlayCartB2B: function openSideNavOverlayCartB2B() {
                if(this.data.isLogged){
                    openSideNavOverlay('.sidenav-overlay_cart', false, {isViewCart: true});
                } else {
                    openSideNavOverlay('.sidenav-overlay_account', true);
                }
            },
            utilityGreeting: function utilityGreeting() {
                if (!this.data.user) return 'Olá';
                var parts = String(this.data.user).trim().split(' ');
                var first = parts[0] || '';
                return first ? ('Olá, ' + first) : 'Olá';
            },
            checkLoginLogged: function checkLoginLogged() {
                if (this.data.isLogged) {
                    var elsShowcaseBtnLoginBuy = document.querySelectorAll('.login-btn_logged');
                    for (var i = 0; i < elsShowcaseBtnLoginBuy.length; i++) {
                        elsShowcaseBtnLoginBuy[i].classList.remove('d-hide');
                    }
                } else {
                    var elsShowcasePriceLogin = document.querySelectorAll('.login-btn_not_logged');
                    for (var i = 0; i < elsShowcasePriceLogin.length; i++) {
                        elsShowcasePriceLogin[i].classList.remove('d-hide');
                    }
                }
            },
        }
    }
    window.__SITE_DOAR_CUSTOMER__ = {{ customer | json }} || null;
    const customer = window.__SITE_DOAR_CUSTOMER__;
    if (typeof mb_track !== 'undefined') {
        mb_track('setRecoveryUrl', 'https://{{ store.url }}/checkout');
    }
    if (customer) {
        if (typeof mb_track !== 'undefined') {
            window.mb_track('accountSync', {
                user: {
                    user_id: customer?.customerId,
                    email: customer?.email,
                    phone: customer?.cellphone,
                    name: customer?.name || customer?.fantasyName,
                }
            });
        }
    }
</script>
{% block script_footer %}{% endblock %}
{%- for script in scripts.footer -%}
    {{ script.code }}
{%- endfor -%}

<!-- Popup Diagnóstico Clima 360 -->
<style>
  #diag360backdrop {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 2147483646;
    background: rgba(0,0,0,0.55);
    -webkit-tap-highlight-color: transparent;
  }
  #diag360backdrop.show { display: block; }
  #diag360pop {
    display: none;
    position: fixed;
    bottom: 28px;
    right: 28px;
    z-index: 2147483647;
    width: 320px;
    max-width: calc(100vw - 32px);
    border-radius: 20px;
    background: linear-gradient(145deg, #111827, #0f172a);
    border: 1px solid rgba(14,165,233,0.4);
    box-shadow: 0 24px 64px rgba(0,0,0,0.7);
    padding: 24px;
    font-family: Arial, sans-serif;
    color: #f1f5f9;
    -webkit-transform: translateZ(0);
    transform: translateZ(0);
  }
  #diag360pop.show {
    display: block !important;
    animation: d360in 0.4s cubic-bezier(0.34,1.56,0.64,1) forwards;
  }
  @keyframes d360in {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  #d360close {
    position: absolute; top: 12px; right: 14px;
    background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.12);
    border-radius: 50%; width: 28px; height: 28px;
    display: flex; align-items: center; justify-content: center;
    cursor: pointer; color: #94a3b8; font-size: 14px; line-height: 1;
  }
  #d360close:hover { background: rgba(255,255,255,0.15); color: #fff; }
  .d360badge {
    display: inline-flex; align-items: center; gap: 6px;
    padding: 5px 10px; border-radius: 999px;
    background: rgba(14,165,233,0.12); border: 1px solid rgba(14,165,233,0.35);
    color: #38bdf8; font-size: 11px; font-weight: 700;
    text-transform: uppercase; letter-spacing: 0.07em; margin-bottom: 14px;
  }
  .d360dot {
    width: 7px; height: 7px; border-radius: 50%;
    background: #0ea5e9; box-shadow: 0 0 6px #0ea5e9;
    animation: d360pulse 1.8s infinite;
  }
  @keyframes d360pulse { 0%,100%{opacity:1} 50%{opacity:.4} }
  .d360title { font-size: 16px; font-weight: 800; line-height: 1.3; margin: 0 0 8px; color: #f1f5f9; }
  .d360text  { font-size: 13px; color: #64748b; line-height: 1.55; margin: 0 0 14px; }
  .d360items { display: flex; flex-direction: column; gap: 6px; margin-bottom: 18px; }
  .d360item  { display: flex; align-items: center; gap: 8px; font-size: 13px; color: #94a3b8; }
  .d360item::before {
    content: "✓"; min-width: 18px; height: 18px; border-radius: 5px;
    background: rgba(14,165,233,0.15); color: #38bdf8; font-size: 11px;
    font-weight: 900; display: flex; align-items: center; justify-content: center;
  }
  .d360cta {
    display: flex; align-items: center; justify-content: center; gap: 8px;
    width: 100%; padding: 13px 20px; border-radius: 12px; border: 0;
    background: linear-gradient(135deg, #0ea5e9, #38bdf8);
    color: #fff !important; font-weight: 700; font-size: 14px;
    cursor: pointer; text-decoration: none !important;
    box-shadow: 0 6px 20px rgba(14,165,233,0.35); margin-bottom: 10px;
  }
  .d360cta:hover { opacity: 0.9; }
  .d360skip {
    display: block; text-align: center; color: #475569; font-size: 12px;
    cursor: pointer; background: none; border: 0; width: 100%;
    font-family: Arial, sans-serif; padding: 4px;
  }
  .d360skip:hover { color: #94a3b8; }
  @media (max-width: 767px) {
    #diag360pop {
      top: 50%;
      bottom: auto;
      left: 16px;
      right: 16px;
      width: auto;
      max-width: 100%;
      transform: translateY(-50%);
      border-radius: 20px;
      padding: 20px;
      box-shadow: 0 12px 48px rgba(0,0,0,0.6);
    }
    #diag360pop.show {
      animation: d360in-mobile 0.4s cubic-bezier(0.34,1.56,0.64,1) forwards;
    }
    @keyframes d360in-mobile {
      from { opacity: 0; transform: translateY(calc(-50% + 20px)); }
      to   { opacity: 1; transform: translateY(-50%); }
    }
  }
</style>

<div id="diag360backdrop" aria-hidden="true"></div>
<div id="diag360pop">
  <button id="d360close" type="button" aria-label="Fechar">&#x2715;</button>
  <div class="d360badge"><span class="d360dot"></span> Novo</div>
  <p class="d360title">Qual ar-condicionado é ideal para você?</p>
  <p class="d360text">Responda 10 perguntas e descubra a recomendação certa para o seu ambiente.</p>
  <div class="d360items">
    <span class="d360item">BTUs ideais para o seu ambiente</span>
    <span class="d360item">Perfil personalizado de climatização</span>
    <span class="d360item">Link direto para os produtos certos</span>
  </div>
  <a class="d360cta" href="/pagina/diagnostico-360">&#127777; Fazer diagnóstico grátis</a>
  <button class="d360skip" id="d360skip" type="button">Agora não</button>
</div>

<script>
(function(){
  try {
    var KEY = 'diag360_shown_session';
    function isDiagPage() {
      var p = (window.location.pathname || '').toLowerCase();
      return p.indexOf('diagnostico') !== -1 || p.indexOf('diag360') !== -1;
    }
    function isMobile() {
      return window.matchMedia('(max-width: 767px)').matches;
    }
    function jaViu() {
      try { return sessionStorage.getItem(KEY) === '1'; } catch(e) { return false; }
    }
    function marcarVisto() {
      try { sessionStorage.setItem(KEY, '1'); } catch(e) {}
    }
    if (jaViu()) return;
    if (isDiagPage()) return;
    var el = document.getElementById('diag360pop');
    var backdrop = document.getElementById('diag360backdrop');
    if (!el) return;
    function fechar(e) {
      if (e) { e.preventDefault(); e.stopPropagation(); }
      el.classList.remove('show');
      el.style.display = 'none';
      if (backdrop) { backdrop.classList.remove('show'); backdrop.style.display = 'none'; }
    }
    function abrir() {
      if (isDiagPage() || jaViu()) return;
      marcarVisto();
      el.classList.add('show');
      el.style.display = 'block';
      if (backdrop && isMobile()) { backdrop.classList.add('show'); backdrop.style.display = 'block'; }
    }
    var btnClose = document.getElementById('d360close');
    var btnSkip = document.getElementById('d360skip');
    if (btnClose) btnClose.addEventListener('click', fechar);
    if (btnSkip) btnSkip.addEventListener('click', fechar);
    if (backdrop) backdrop.addEventListener('click', fechar);
    function foraClick(e) {
      if (!el.classList.contains('show')) return;
      if (el.contains(e.target)) return;
      fechar(e);
    }
    document.addEventListener('mousedown', foraClick);
    document.addEventListener('touchstart', foraClick, { passive: true });
    setTimeout(abrir, isMobile() ? 3000 : 5000);
  } catch(e) {
    console.error('Popup diag360:', e);
  }
})();
</script>

</body>
</html>
