{
  "name": "Site do Ar — Tray/WDNA theme",
  "install": "node --check assets/favorites.js && node --check assets/mega-menu.js && node --check assets/product-hover-image.js && echo 'asset JS syntax OK'",
  "terminals": [
    {
      "name": "preview",
      "command": "cd /workspace && python3 -m http.server 8000 --bind 0.0.0.0"
    }
  ],
  "ports": [8000]
}
